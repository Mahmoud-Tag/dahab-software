# TODO

- [ ] تحديث metadata في `src/app/layout.tsx` ليستخدم `/logo.png` كـ `icons.icon` و `openGraph.images` و `twitter.images`.
- [ ] إضافة ملف بيانات ثابتة في `public/opengraph-image.png` (أو توليدها) إذا كان مطلوباً لصور OG ثابتة.
- [ ] إنشاء `public/icon-192.png` و `public/icon-512.png` (إن لم تكن موجودة) لتتوافق مع `manifest.webmanifest`.
- [ ] تحديث `robots`/`sitemap` إن كانت هناك حاجة لإضافات.
- [ ] تشغيل `npm run build` و `npm run start` للتأكد من عدم وجود أخطاء.
- [ ] اختبار عبر: فتح الصفحة و التأكد من تواجد وسوم: `link rel="icon"` و `meta property="og:image"` و `meta name="twitter:image"`.
- [ ] التحقق في Google Search Console: طلب فهرسة وتحديث الكاش.

